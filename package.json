{
  "scripts": {
    "start": "node test",
    "watch": "nodemon --exec 'node test' --ext 'js'"
  },
  "dependencies": {
    "puppeteer": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5"
  }
}
